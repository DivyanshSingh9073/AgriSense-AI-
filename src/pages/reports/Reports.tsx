import { PageContainer } from '@/components/common';

/** Exportable field and yield reports. */
export function Reports() {
  return <PageContainer title="Reports" description="Generate and export field reports." />;
}
