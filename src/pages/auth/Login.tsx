import { PageContainer } from '@/components/common';

/** Sign-in page, rendered inside AuthLayout. */
export function Login() {
  return <PageContainer title="Sign in" description="Access your AgriSense AI dashboard." />;
}
